# Development Log

## 2026-09-05: этапы проекта как статусы дейли

- Живой тест уточнил рабочий словарь: `К выполнению`, `В работе`, `Готово`.
- Бот динамически читает этапы конкретного проекта из `project.task.type`.
- Кнопки записывают `stage_id`; этап `Готово` также закрывает техническое состояние.
- В ответе и outbox сохраняются и этап, и внутреннее состояние Odoo.

## 2026-09-05: управляемый повтор E2E

- Закрытая тестовая задача корректно исчезла из `/daily` после состояния `Готово`.
- Добавлена локальная команда `/test_reopen <task_id>` для повторяемой приёмки.
- Команда проверяет назначение задачи и заблокирована во всех нелокальных окружениях.

## 2026-09-05: видимость результатов в Odoo

- Подтверждено отображение дейли-комментариев во внутреннем chatter задачи.
- Ссылки участника направлены в backend-карточку вместо клиентского portal preview.
- Для новых `message_post` включён `body_is_html`, чтобы Odoo не показывал HTML-теги.
- Telegram явно различает канбан-этап и внутреннее состояние задачи Odoo.

## 2026-09-05: стабильная идемпотентность сотрудника

- TC-04 выявил новый Odoo-комментарий после смены Telegram-привязки сотрудника.
- Ключ daily outbox переведён с изменяемого Telegram ID на Odoo user ID.
- Добавлено автоматическое принятие последней legacy-операции без нового комментария.
- Регрессионный тест проверяет миграцию обоих ключей: статуса и комментария.

## 2026-09-05: диагностика E2E без веб-интерфейса Odoo

- Из-за медленной загрузки test Odoo добавлен безопасный `scripts/check-test.cmd`.
- Скрипт показывает ответы, завершение дейли и состояния outbox без вывода секретов.
- Успешный `SENT` используется как подтверждение принятия операции Odoo API.

## 2026-09-05: повторная привязка тестового пользователя

- Живой TC-02 нашёл конфликт уникальности для сохранённого ранее Odoo user `94`.
- Регистрация теперь повторно использует существующую запись Odoo и обновляет её
  Telegram-привязку вместо создания конфликтующего дубля.
- Добавлен регрессионный тест сценария на уже существующей тестовой базе.

## 2026-09-05: исправление контейнерного запуска

- По журналу живого теста обнаружена ошибка валидации пустого `PUBLIC_BASE_URL`.
- Пустое значение необязательного URL теперь корректно преобразуется в `None`.
- Добавлен регрессионный тест конфигурации и улучшена диагностика Windows-запуска.
- Исправлена ложная ошибка Windows-проверки при реально работающем polling-контейнере.

## 2026-09-05: версионирование схемы PostgreSQL

- Добавлена начальная Alembic-миграция для всех таблиц приложения и индекса задач.
- Docker-образ запускает `alembic upgrade head` до старта Telegram polling.
- GitHub Actions применяет миграцию к чистой PostgreSQL при каждом изменении.
- В регламент обновления добавлена обязательная резервная копия PostgreSQL.
- Линтер и компиляция миграционного окружения проходят локально.

## 2026-09-04

Выполнено:

- разобрано ТЗ и выделены пользовательские сценарии US1-US6;
- исследованы официальные API Odoo, Telegram и OpenAI;
- определены версии test/prod Odoo и доступность XML-RPC;
- проверена переданная test-учётная запись без сохранения секрета;
- спроектированы архитектура, модель данных и outbox pattern;
- реализованы конфигурация, Odoo-клиент и read-only discovery-команда;
- реализованы SQLAlchemy-модели и retry/backoff для очереди Odoo;
- добавлены тесты prompt contract, backoff и расписания;
- создан приватный GitHub-репозиторий и первичный backlog;
- настроен GitHub Actions CI.
- реализован интерактивный Telegram demo-сценарий для проверки без Odoo.
- собран Docker-образ и подтверждена работа Telegram polling в контейнере;
- подготовлена production-конфигурация VPS с закрытой PostgreSQL, restart policy,
  ротацией логов и отдельным регламентом развёртывания.
- подтверждён веб-вход в Odoo 18 test stand с email 2FA;
- создан временный test API key и успешно пройден XML-RPC discovery;
- создан изолированный проект, стадии и назначенная задача для E2E-проверок;
- подтверждены API-операции чтения, смены состояния/этапа и `message_post` с URL;
- Telegram daily-сценарий подключён к чтению и обновлению реальных задач Odoo.

Проверено локально:

- все Python-файлы компилируются на Python 3.12;
- реальные пароли и токены отсутствуют в отслеживаемых файлах;
- локальная Git-ветка чистая после содержательных коммитов.

Открытые зависимости:

- нужен API key AI-провайдера для настоящих рекомендаций вместо deterministic demo;
- нужна таблица соответствия Telegram-пользователей пользователям Odoo;
- для постоянной работы нужен VPS или совместимая контейнерная платформа;
- перед production требуется отдельный integration user/key и проверка кастомных
  моделей связанного портфеля.

Следующий технический шаг: прогнать Telegram E2E на задаче `id=4`, затем подключить
PostgreSQL/outbox к FSM для идемпотентности и гарантированной доставки.
## 2026-09-05: индивидуальная привязка участников

- Добавлена конфигурация `TELEGRAM_ODOO_USER_MAP` в формате JSON.
- `/start`, `/daily`, `/weekly`, ответы и затруднения используют Odoo ID отправителя.
- Ненастроенный пользователь не может прочитать или изменить задачи тестового default.
- `ODOO_DEFAULT_USER_ID` оставлен только для одиночного E2E на test stand.
- Production Compose и документация дополнены примером командной карты.
- Добавлен unit-тест приоритета индивидуальной привязки над default.

## 2026-09-05: воспроизводимый Windows-запуск

- Добавлен `scripts/start-test.cmd` для запуска тестовой конфигурации без ручного ввода
  Docker-команд.
- Скрипт проверяет `.env` и Docker Engine, заменяет только старый контейнер проекта,
  собирает актуальный образ и выводит `docker compose ps`.
- Секреты не выводятся и остаются в локальном `.env`.

## 2026-09-05: протокол приёмочного тестирования

- Добавлен `docs/e2e_test_plan.md` с одиннадцатью сценариями US1-US6.
- Для каждого кейса определены предусловия и наблюдаемый результат в Telegram,
  PostgreSQL и test Odoo.
- Добавлены проверки retry, идемпотентности, ролей и отсутствия дублей.
- Определено правило доказательств для отчёта без секретов и персональных данных.

## 2026-09-05: корректное завершение дейли

- Добавлена таблица `daily_summaries` с уникальностью `(user, summary_date)`.
- Общий ответ вне списка задач теперь сохраняется, включая явное отсутствие текста.
- Дайджест и напоминания считают ответившим только завершившего весь опрос участника.
- Дата ответа вычисляется в `Asia/Yekaterinburg`, а не по UTC.
- Повторное завершение в тот же день обновляет существующую запись.

## 2026-09-04: уведомления о назначении задачи

- Добавлен fallback polling открытых Odoo-задач раз в минуту.
- Первый запуск создаёт базовый `task_cache` без рассылки старых назначений.
- Следующие изменения `user_ids` сравниваются с предыдущим снимком.
- Новый исполнитель получает название, проект, дедлайн и ссылку на карточку.
- Добавлены тесты вычисления множества новых исполнителей.
- Webhook остаётся будущей оптимизацией после появления публичного HTTPS endpoint.

# 2026-09-04: рабочее расписание и эскалации

- APScheduler подключён к реальным async-функциям вместо заглушек.
- В 09:00 по понедельникам участники получают приглашение пройти `/weekly`.
- В 12:00 по будням участники получают приглашение пройти `/daily`.
- В 14:00 неответившим отправляется адресное напоминание.
- В 10:00 PM и техлидам рассылается дневной дайджест.
- В 18:00 открытые затруднения старше двух дней эскалируются с AI-советом.
- `/start` регистрирует пользователя для плановых рассылок.

# 2026-09-04: команды PM и техлида

- Добавлен allowlist ролей через `PM_TELEGRAM_IDS` и `TECH_LEAD_TELEGRAM_IDS`.
- Реализованы `/status`, `/deadlines`, `/remind`, `/digest` и `/digest week`.
- Статус ищется сначала по участнику в локальной истории, затем по названию Odoo-задачи.
- Дайджест показывает ответивших, неответивших, открытые затруднения и AI-советы.
- На test stand проверены read-only домены поиска задач и дедлайнов.
- Добавлен unit-тест разбора списков Telegram ID.

# 2026-09-04: жизненный цикл затруднения

- Затруднение и рекомендация сохраняются в `blockers` и `ai_advices` сразу после ввода.
- В Odoo через outbox создаётся отдельное chatter-сообщение с рекомендацией.
- Повторное описание открытой проблемы по той же задаче обновляет запись без дубля.
- Добавлены `/blockers`, кнопка снятия затруднения и необязательная ссылка на решение.
- После снятия обновляется исходное chatter-сообщение, а прерванный дейли продолжается.
- Добавлен тест HTML-экранирования текста затруднения и ссылки.

# 2026-09-04: недельное планирование

- Добавлена команда `/weekly` и отдельная FSM.
- Фокус выбирается только из открытых Odoo-задач, поддерживается множественный выбор.
- После подтверждения бот запрашивает необязательный стратегический текст.
- План обновляется по уникальному ключу `(user, week_start)` без дублей.
- Добавлены тесты вычисления начала недели и клавиатуры выбора задач.

# 2026-09-04: надёжная доставка ответов

- Telegram FSM подключён к таблицам `users`, `daily_answers` и `odoo_outbox`.
- Ответ сохраняется до вызова Odoo; временная ошибка переводит операцию в retry.
- Ключ `(Telegram user, Odoo task, date)` обновляет существующий ответ за день.
- ID chatter-сообщения сохраняется, поэтому повторное прохождение редактирует его.
- При запуске бот создаёт отсутствующие таблицы и поднимает outbox worker.
- Dev Compose дополнен PostgreSQL healthcheck и корректным внутренним `DATABASE_URL`.

# 2026-09-04: AI-совет и контекст Odoo

- Реализован асинхронный вызов OpenAI Responses API с `store=False`.
- В запрос передаются задача, проект, стадия, дедлайн, время в стадии, текст
  затруднения и пять последних комментариев только по этой задаче.
- Добавлен явный локальный fallback на случай отсутствия ключа или временной ошибки AI.
- На test stand проверено чтение истории `mail.message` для тестовой задачи.
- Добавлены unit-тесты контракта AI, вычисления контекста и очистки HTML из chatter.
- Живой AI-вызов отложен до получения отдельного `OPENAI_API_KEY`.

